name: 'retail_pipeline'
version: '1.0.0'
config-version: 2
profile: 'retail_pipeline'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  retail_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table
